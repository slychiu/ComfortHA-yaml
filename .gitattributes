# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts and sourceable config files must always use LF — CRLF breaks bash on Linux
*.sh text eol=lf
*.example text eol=lf
.gitignore text eol=lf
