{
  "version": 7,
  "changelog": "Fixed v6's duplicate-click guard, which never actually fired: it checked .zero_touch_completed, which gets touched ~15-30s into the cycle (before the second HA restart from finish_firstboot.sh, ~3-4 min later) rather than at true completion. Now uses a dedicated flag set at the start of dev_reset.sh and cleared at the true end of the full two-restart cycle.",
  "warnings": [],
  "files": [
    "first_boot.sh",
    "reset.sh",
    "dev_reset.sh",
    "cytech_reject_update.sh",
    "cytech_check_only.sh",
    "packages/cytech.yaml",
    "cytech_update.sh"
  ]
}
