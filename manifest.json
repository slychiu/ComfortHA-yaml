{
  "version": 11,
  "changelog": "Fixed finish_firstboot.sh stopping its own SSH addon container before finishing its cleanup steps -- it runs *inside* that container, so stopping it could kill the script before it cleared the provisioning SSH key or the Reset to Default debounce flag (.reset_cycle_active), silently leaving both in a bad state. Reordered so cleanup happens before the self-stop, not after.",
  "warnings": [],
  "files": [
    "first_boot.sh",
    "reset.sh",
    "dev_reset.sh",
    "cytech_reject_update.sh",
    "cytech_check_only.sh",
    "packages/cytech.yaml",
    "cytech_update.sh",
    "welcome_dashboard.json"
  ]
}
