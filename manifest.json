{
  "version": 5,
  "changelog": "Reset to Default dashboard now ships to every device (not just ones set up manually); Config Files page shows the full changelog and warnings for a pending update, not just the version number; fixed a bug where long update messages silently showed as blank/unknown due to Home Assistant's 255-character sensor state limit",
  "warnings": [
    "Config Files dashboard layout will be refreshed to show pending-update details -- if you customized it, back up your changes first."
  ],
  "files": [
    "first_boot.sh",
    "reset.sh",
    "dev_reset.sh",
    "cytech_reject_update.sh",
    "cytech_check_only.sh",
    "packages/cytech.yaml",
    "cytech_update.sh"
  ]
}
